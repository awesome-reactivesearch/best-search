import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("best-search-root"));

const Main = () => {
  return <App />;
};

root.render(<Main />);

/* Use this file for testing showcase*/
